interface InputFieldConfig {
    type: string,
    placeholder: string,
    id?: string,
    className?: string|Array<string>,
    name?: string,
    isRequired: bool
}

class nuInputs {
    public createInputField(root: HTMLElement, config: InputFieldConfig) {

    }
}